server:
  port: 8080
spring:
  application:
    name: demo
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/cux?useUnicode=true&characterEncoding=UTF8
    username: cux
    password: cux123
  jpa:
    # 打印sql
    show-sql: true
    # 格式化 sql
    properties:
      hibernate:
        format_sql: true
springdoc:
  swagger-ui:
    path: /apiui